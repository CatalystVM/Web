<script setup lang="ts">
const route = useRoute()
const config = useRuntimeConfig()

definePageMeta({})

useHead({
    title: `Become a Vendor | ${config.app.name}`
})
</script>

<template>
    <div>

    </div>
</template>
